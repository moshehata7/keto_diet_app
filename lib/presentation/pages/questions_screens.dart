import 'package:flutter/material.dart';

class QuestionsScreens extends StatelessWidget {
  const QuestionsScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("أسئلة و أجوبة  "),
      ),
    );
  }
}