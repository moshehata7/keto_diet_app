import 'package:flutter/material.dart';

class KetoFoodScreen extends StatelessWidget {
  const KetoFoodScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("المسموح و الممنوع في الكيتو"),
      ),
    );
  }
}